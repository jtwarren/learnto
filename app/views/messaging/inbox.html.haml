%h1 Inbox
-for conversation in @inbox
	-for message in conversation.messages
		%h4 subject
		=message.subject
		%h4 body
		=message.body
		%h4 reply
		= form_tag reply_path do
			=text_area_tag :replytext
			=hidden_field_tag :conversation_id, conversation.id
			=submit_tag "Reply"

