- provide(:title, "Lessons - LearnTo")

/ %ul.nav.nav-pills.nav-justified
/   %li.active
/     = link_to 'Public lessons', "#"
/   %li
/     = link_to 'Your networks', "#"
/   %li
/     = link_to 'Upcoming Events', "#"


- if not @events.empty?
  %h2.heading
    Upcoming Lessons in Cambridge
  .grid
    .row
      - @events.each do |event|
        .col-xs-12.col-sm-6.col-md-4
          = render partial: "events/event_short", locals: {event_short: event}

%a#skills
%h2.heading
  Top Lessons in Cambridge
.grid
  .row
    - @skills.each do |skill|
      .col-xs-12.col-sm-6.col-md-4
        = render partial: "skill_short", locals: {skill_short: skill}

.new
  Can't find what you're looking for?  #{mail_to "founders@learnto.com", "Let us know", subject: "LearnTo Lesson Request"}, and we'll find you a teacher!

- if current_user
  #aboutMeModal.modal.fade{"aria-hidden" => "true", "aria-labelledby" => "myModalLabel", role: "dialog", tabindex: "-1"}
    .modal-dialog
      .modal-content
        .modal-header
          %button.close{"aria-hidden" => "true", "data-dismiss" => "modal", type: "button"} ×
          %h4#myModalLabel.modal-title Last step! Tell us a little about yourself!
        = form_for(@user) do |f|
          .modal-body
            .form-group
              = f.label :bio, "Biography"
              %p.help-block
                Tell others a little bit about yourself.  This makes them more likely to learn and teach with you.
              .input-group
                %span.input-group-addon
                  %span.glyphicon.glyphicon-user
                = f.text_area :bio, rows: 6, class: "form-control"
            %hr.div-line
            .form-group
              = f.label :location, "Location"
              %p.help-block
                Where are you currently located?  It helps others find classes near them!
              .input-group
                %span.input-group-addon
                  %span.glyphicon.glyphicon-map-marker
                = f.text_field :location, class: "form-control"
            %hr.div-line
            .form-group
              = f.label :school, "School"
              %p.help-block
                Where do you, or have you gone to school?
              .input-group
                %span.input-group-addon
                  %span.glyphicon.glyphicon-book
                = f.text_field :school, class: "form-control"
            %hr.div-line
            .form-group
              = f.label :work, "Work"
              %p.help-block
                Where do you, or have you worked?  Others find this information useful.
              .input-group
                %span.input-group-addon
                  %span.glyphicon.glyphicon-briefcase
                = f.text_field :work, class: "form-control"
            %hr.div-line
            .form-group
              = f.label :network_ids, "Networks"
              %p.help-block
                Which networks are you apart of?
              .input-group
                %span.input-group-addon
                  %span.glyphicon.glyphicon-cloud
                = f.select :network_ids, Network.all.collect {|x| [x.name, x.id]}, {}, :multiple => true, class: "form-control"
          .modal-footer
            = f.submit "Submit", class: "btn btn-custom"
            = link_to "Cancel", "#", class: "btn btn-custom", data: {dismiss: "modal"}


- if @show_user_bio
  %script
    $(aboutMeModal).modal()