%a.skills
- provide(:title, "Lessons - LearnTo")
%h2.heading
  Top Lessons in Cambridge

.grid
  .row
    = @skills.each_slice(3) do |slice|
      - slice.each do |skill|
        .col-xs-12.col-sm-6.col-md-4
          = render partial: "skill_short", locals: {skill_short: skill}

.new
  Can't find what you're looking for?  #{mail_to "founders@learnto.com", "Let us know", subject: "LearnTo Lesson Request"}, and we'll find you a teacher!

- if current_user
  #aboutMeModal.modal.fade{"aria-hidden" => "true", "aria-labelledby" => "myModalLabel", role: "dialog", tabindex: "-1"}
    .modal-dialog
      .modal-content
        .modal-header
          %button.close{"aria-hidden" => "true", "data-dismiss" => "modal", type: "button"} ×
          %h4#myModalLabel.modal-title Last step! Tell us a little about yourself!
        .modal-body
          = form_for(current_user, html: { class: "form-horizontal" } ) do |f|
            .form-group
              = f.text_area :bio, class: "form-control", rows: "4", columns: "70", required: true, placeholder: "Describe Yourself - LearnTo is a community built on relationships. Help others get to know you! What are 3 things you've always wanted to learn? What are your favorite hobbies, topics, or activites?"
            .form-group
              = f.text_field :school, class: "form-control", placeholder: "School"
            .form-group
              = f.text_field :work, class: "form-control", placeholder: "Work"
            = f.hidden_field :return_to, value: skills_url(anchor: "skills")
            = f.hidden_field :return_to, value: skills_url(anchor: "skills")
            = f.submit "Submit", class: "btn btn-custom"

-if not @hide_about_me
  %script
    $(aboutMeModal).modal()