.row
  .col-md-10
    %h4 #{title}
  -if user == current_user
    .col-md-2.edit-col
      .edit.btn.btn-md.btn-custom
        Edit
.text
  = field_value
- if user == current_user
  .edit-field
    %textarea.form-control
      = field_value
    %br
    .save.btn.btn-md.btn-custom
      Save