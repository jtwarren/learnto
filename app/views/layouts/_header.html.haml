%nav.navbar.navbar-default.navbar-static-top{role: "navigation"}
  .container
    .navbar-header
      %button.navbar-toggle{"data-target" => "#collapse_links", "data-toggle" => "collapse", type: "button"}
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      = link_to (image_tag 'learnto-400.png', :class => 'nav-logo'), root_path
    #collapse_links.collapse.navbar-collapse.right-links
      %ul.nav.nav-pills.navbar-right
        %li
          = link_to 'Teach', new_skill_path
        - if current_user
          %li
            = link_to current_user.name, user_path(current_user)
          %li
            .unread.badge.badge-error.pull-right
              =link_to current_user.unread, inbox_conversations_path
        - else
          %li
            = link_to "Login", login_path