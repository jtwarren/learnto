%h2.heading
  Requested Lessons
.grid
  = @lessons.each_slice(3) do |slice|
    - slice.each do |lesson|
      .col-xs-12.col-sm-6.col-md-4
        = render partial: "lesson_short", locals: {lesson_short: lesson}